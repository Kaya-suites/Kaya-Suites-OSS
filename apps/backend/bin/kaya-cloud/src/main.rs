//! kaya-cloud — hosted cloud binary (BSL 1.1)
//!
//! This binary depends on all BSL crates (billing, tenant, metering,
//! postgres-storage). It is stripped from the public mirror before sync.
//!
//! Not yet implemented — placeholder for license-boundary scaffolding.

fn main() {
    eprintln!("kaya-cloud: not yet implemented");
    std::process::exit(1);
}
